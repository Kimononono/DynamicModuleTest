const ImageComponent = () => {
  return <img src="https://via.placeholder.com/150" alt="Placeholder" />;
};

ImageComponent.metadata = {
  name: 'ImageComponent',
  description: 'A placeholder image component',
  type: 'Media',
  tags: ['image', 'media', 'visual'],
};

export default ImageComponent;

